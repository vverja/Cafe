#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗагрузитьКурсыКлиент", ЭтотОбъект);
	ПоказатьВопрос(ОписаниеОповещения, 
		НСтр("ru='Будет произведена загрузка файла с полной информацией по курсами всех валют за все время из менеджера сервиса.
              |Курсы валют, помеченных в областях данных для загрузки из сети Интернет, будут заменены в фоновом задании. Продолжить?'
              |;uk='Буде проведене завантаження файлу з повною інформацією про курси всіх валют за весь час з менеджера сервісу.
              |Курси валют, позначених в областях даних для завантаження з мережі Інтернет, будуть замінені у фоновому завданні. Продовжити?'"), 
		РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ЗагрузитьКурсыКлиент(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Нет Тогда
		Возврат;
	КонецЕсли;
	
	ЗагрузитьКурсы();
	
	ПоказатьОповещениеПользователя(
		НСтр("ru='Загрузка запланирована.';uk='Завантаження заплановане.'"), ,
		НСтр("ru='Курсы будут загружены в фоновом режиме через непродолжительное время.';uk='Курси будуть завантажені у фоновому режимі через нетривалий час.'"),
		БиблиотекаКартинок.Информация32);
	
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьКурсы()
	
	КурсыВалютСлужебныйВМоделиСервиса.ЗагрузитьКурсы();
	
КонецПроцедуры

#КонецОбласти
