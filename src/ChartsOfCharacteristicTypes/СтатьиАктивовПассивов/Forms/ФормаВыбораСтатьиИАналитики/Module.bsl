
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;

	Если Параметры.Свойство("АктивПассив") Тогда
		ПризнакАктивПассив = Параметры.АктивПассив;
		Если ПризнакАктивПассив = Перечисления.ВидыСтатейУправленческогоБаланса.Актив Тогда
			Элементы.Статья.Заголовок = НСтр("ru='Статья активов';uk='Стаття активів'");
			Заголовок = НСтр("ru='Выбор статьи активов и аналитики';uk='Вибір статті активів та аналітики'");
		Иначе
			Элементы.Статья.Заголовок = НСтр("ru='Статья пассивов';uk='Стаття пасивів'");
			Заголовок = НСтр("ru='Выбор статьи пассивов и аналитики';uk='Вибір статті пасивів та аналітики'");
		КонецЕсли;
		
		ПараметрВыбора = Новый ПараметрВыбора("Отбор.АктивПассив", Параметры.АктивПассив);
		МассивПараметровВыбора = Новый Массив;
		МассивПараметровВыбора.Добавить(ПараметрВыбора);
		Элементы.Статья.ПараметрыВыбора = Новый ФиксированныйМассив(МассивПараметровВыбора);
	Иначе
		Заголовок = НСтр("ru='Выбор статьи и аналитики';uk='Вибір статті та аналітики'");
	КонецЕсли;
		
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Выбрать(Команда)
	
	Если ПроверитьЗаполнение() Тогда
		Параметр = Новый Структура("Статья, Аналитика", СтатьяАктивовПассивов, АналитикаАктивовПассивов);
		Закрыть(Параметр);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
