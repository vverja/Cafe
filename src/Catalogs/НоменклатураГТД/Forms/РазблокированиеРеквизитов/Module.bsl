#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	РазрешитьРедактированиеКодУКТВЭД = Истина;
	РазрешитьРедактированиеНомераГТД = Истина;
	
КонецПроцедуры // ПриСозданииНаСервере()

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура РазрешитьРедактирование(Команда)
	
	Результат = Новый Массив;
	Если РазрешитьРедактированиеКодУКТВЭД Тогда
		Результат.Добавить("КодУКТВЭД");
	КонецЕсли;
	
	Если РазрешитьРедактированиеНомераГТД Тогда
		Результат.Добавить("НомерГТД");
	КонецЕсли;
	
	Закрыть(Результат);
	
КонецПроцедуры // РазрешитьРедактирование()

#КонецОбласти

