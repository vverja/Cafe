#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Элементы.ФормаСоздатьБизнесРегионы.Видимость = ПравоДоступа("Изменение", Метаданные.Справочники.БизнесРегионы);
	
	МожноРедактировать = ПравоДоступа("Редактирование", Метаданные.Справочники.БизнесРегионы);
	Элементы.ФормаИзменитьВыделенные.Видимость = МожноРедактировать;
	Элементы.СписокКонтекстноеМенюИзменитьВыделенные.Видимость = МожноРедактировать;
	
	// МенюОтчеты
	МенюОтчеты.ПриСозданииНаСервере(ЭтотОбъект, Элементы.ПодменюОтчеты);
	// Конец МенюОтчеты
	
	СобытияФорм.ПриСозданииНаСервере(ЭтаФорма, Отказ, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ИзменитьВыделенные(Команда)
	
	ГрупповоеИзменениеОбъектовКлиент.ИзменитьВыделенные(Элементы.Список);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьБизнесРегионы(Команда)
	
	Если Не ГеографическаяСхемаДляОтчетовЗаполнена() Тогда
		ТекстПредупреждения = НСтр("ru='Не выбрана карта в разделе администрирования ""CRM и продажи"". Географические регионы не могут быть заполнены.';uk='Не обрана карта в розділі адміністрування ""CRM і продажі"". Географічні регіони не можуть бути заповнені.'");
		ПоказатьПредупреждение(Неопределено, ТекстПредупреждения);
	Иначе
		ОписаниеОповещения = Новый ОписаниеОповещения("ОбновитьСписокБизнесРегионов", ЭтотОбъект);
		ОткрытьФорму("Справочник.БизнесРегионы.Форма.ФормаГенерацииБизнесРегионов",, ЭтаФорма,,,, ОписаниеОповещения,);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ВыполнитьПереопределяемуюКоманду(Команда)
	
	СобытияФормКлиент.ВыполнитьПереопределяемуюКоманду(ЭтаФорма, Команда);
	
КонецПроцедуры

// МенюОтчеты
&НаКлиенте
Процедура Подключаемый_ВыполнитьКомандуОтчет(Команда)
	
	МенюОтчетыКлиент.ВыполнитьПодключаемуюКомандуОтчет(Команда, ЭтотОбъект, Элементы.Список);
	
КонецПроцедуры
// Конец МенюОтчеты

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ОбновитьСписокБизнесРегионов(РезультатЗакрытия, ДополнительныеПараметры) Экспорт
	
	Если ТипЗнч(РезультатЗакрытия) = Тип("Число") Тогда
		Если РезультатЗакрытия Тогда
			Элементы.Список.Обновить();
			ТекстОповещенияПользователя = НСтр("ru='Записано новых бизнес-регионов: %1';uk='Записано нових бізнес-регіонів: %1'");
			ТекстОповещенияПользователя = 
				СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстОповещенияПользователя, РезультатЗакрытия);
		Иначе
			ТекстОповещенияПользователя = НСтр("ru='Все выбранные регионы найдены.';uk='Всі вибрані регіони знайдено.'");
		КонецЕсли;
		ПоказатьОповещениеПользователя(ТекстОповещенияПользователя);
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ГеографическаяСхемаДляОтчетовЗаполнена()
	
	Возврат ЗначениеЗаполнено(Константы.ГеографическаяСхемаДляОтчетов.Получить().Получить());
	
КонецФункции

#КонецОбласти

