#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Обработка обновления
//
Процедура ЗаполнитьКонстантуНеИспользоватьЖурналПлатежей() Экспорт
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьЖурналПлатежей") =
			ПолучитьФункциональнуюОпцию("НеИспользоватьЖурналПлатежей") Тогда
		// Опции должны иметь противоположные значения.
		// Если это не так, то значит в ИБ раньше не было этих опций - инициализируем их значения.
		Константы.НеИспользоватьЖурналПлатежей.Установить(Не Константы.ИспользоватьЖурналПлатежей.Получить());
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли