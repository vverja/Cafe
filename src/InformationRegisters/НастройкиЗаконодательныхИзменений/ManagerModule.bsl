Функция ЗначениеНастройки(ПараметрНастройки, Период = Неопределено) Экспорт
	
	Результат =  РегистрыСведений.НастройкиЗаконодательныхИзменений.ПолучитьПоследнее(Период, 
		Новый Структура("ПараметрНастройки", Перечисления.ПараметрыЗаконодательныхИзменений[ПараметрНастройки])).ЗначениеПараметра;
		
	Если Результат = Неопределено Тогда
		
		Если ПараметрНастройки = "ДатаНачалаИспользованияФормыНН15" Тогда
			Результат = '20230801';
		КонецЕсли;
		
	КонецЕсли;
		
	Возврат Результат;
	
КонецФункции